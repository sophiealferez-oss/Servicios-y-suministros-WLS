# ===========================================
# CONFIGURACIÓN PARA VERCEL + NEON
# ===========================================

# Database Configuration (Neon PostgreSQL)
# Obtén tu DATABASE_URL desde https://console.neon.tech
# Formato: postgresql://user:password@host.neon.tech/database?sslmode=require
DATABASE_URL=postgresql://user:password@host.neon.tech/database?sslmode=require

# JWT Secret (cambia esto a una cadena aleatoria segura)
# Genera una con: openssl rand -base64 32
JWT_SECRET=tu_secreto_muy_seguro_aqui_cambia_esto

# Port (Vercel usa su propio puerto, esto es solo para desarrollo local)
PORT=3000

# Host (0.0.0.0 para desarrollo local)
HOST=0.0.0.0

# Node Environment
NODE_ENV=development
