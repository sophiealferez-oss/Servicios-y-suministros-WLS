# Database Configuration
# Railway proporciona DATABASE_URL automáticamente cuando agregas PostgreSQL
# Formato: postgresql://user:password@host:port/database
DATABASE_URL=postgresql://user:password@host:port/database

# JWT Secret (cambia esto a una cadena aleatoria segura)
JWT_SECRET=tu_secreto_muy_seguro_aqui_cambia_esto

# Port (Railway usa su propia variable PORT, pero puedes definir un fallback)
PORT=3000

# Host (0.0.0.0 para escuchar en todas las interfaces - requerido para Railway)
HOST=0.0.0.0

# Node Environment
NODE_ENV=production
